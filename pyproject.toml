[project]
name = "hexlet-hello-world"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Aleksandr Buzov", email = "abuzov@list.ru" }
]
requires-python = ">=3.14"
dependencies = [
    "more-itertools>=11.1.0",
]

[project.scripts]
hexlet-hello-world = "hexlet_hello_world:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
